Hey   ....   
Devloper This is DW 
follow   my guides  to   run this  project.

Step 1 :- Download the project.
Step 2 :- Unzip it
Step 3 :- Open in pycharm 
Step 4 :- Open the terminal (Note Check install all the libarires - )*
        pip install Streamlit
        pip install serial
        pip install yolo
        pip install Ultralytics
        pip install openCV
        if any intall brooooo
Step 5 :- copy paste in terminal ( python -m streamlit run app.py )
